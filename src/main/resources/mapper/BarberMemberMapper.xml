<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.nicecoder.barbersys.mapper.BarberMemberMapper">
    <select id="memberStatisMonth" resultType="cn.nicecoder.barbersys.entity.PO.OverviewPO">
        SELECT
            count(id) countNum,
            MONTH(create_time) createTime
        FROM
            barber_member
        WHERE
            YEAR(create_time)=YEAR(NOW())
        GROUP BY
            MONTH(create_time)
        ORDER BY
            MONTH(create_time) DESC;
    </select>

    <select id="memberStatisCurrentMonth" resultType="cn.nicecoder.barbersys.entity.PO.OverviewPO">
        SELECT
            count(id) countNum,
            MONTH(create_time) createTime
        FROM
            barber_member
        WHERE
            DATE_FORMAT(create_time, '%Y%m' ) = DATE_FORMAT( CURDATE( ) , '%Y%m' )
        GROUP BY
            MONTH(create_time)
        ORDER BY
            MONTH(create_time) DESC;
    </select>
</mapper>
